type Location {
  pathname: String
  search: String
  origin: String
  hash: String
  host: String
  hostname: String
  href: String
  port: Int
  protocol: String
  todoId: ID
}

extend type Query {
  location: Location
}
